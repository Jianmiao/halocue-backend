# 外部依赖

## AA 兼容运行依赖

当前 AA 兼容层保持 `local-only`，不属于 HaloCue 仓库内容。

| 字段 | 值 |
|---|---|
| 依赖 ID | `aa-compat-local` |
| 固定坐标 | `handoff-aa-compat/2026-08-15` |
| 默认本地路径 | `01-完整程序/aa` |
| 环境变量覆盖 | `HALOCUE_LEGACY_ROOT` |
| 用途 | AA 兼容测试、`.aap` 编译和本地互操作预检 |
| 仓库策略 | 不提交、不上传、不分发 |
| 许可证状态 | 未确认 |

固定坐标由原交接清单中 `01-完整程序/aa/` 范围的 76 项记录生成。按原顺序使用 LF 连接，并保留末尾 LF 后计算 SHA-256：

```text
sha256:67d6d1f093cc1d07d5922e1add1020caf49a7a2ffb3b8ca0bd3db644897dbc9f
```

该摘要只用于确认本地依赖是否与 B0 验证环境一致，不授予源码、数据库、素材或品牌资源的复制与分发权。原始清单和 AA 文件均由 `.gitignore` 排除。

在获得可分发的独立适配器包或仓库前：

- 新克隆环境不会自动获得 AA 依赖。
- CI 不得下载或重建该依赖。
- 缺少依赖时，AA capability 必须报告未配置；写作域、正式合同校验和其他非 AA 功能应继续工作。
- 不得把本地路径或依赖内容暴露到 API、日志或 BuildBundle。
